In order to run experiment_xml_to_kml.py you need the following:

Python (tested with 2.7.3)	python
IDLE (if you want an IDE)	idle
lxml				python-lxml
pip				python-pip

You can then install pyKML using PIP:

sudo pip install pykml

Should be good to go!
